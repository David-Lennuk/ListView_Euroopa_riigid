<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ListView_Euroopa_riigid.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ListView_Euroopa_riigid"
    Shell.FlyoutBehavior="Disabled"
    Title="ListView_Euroopa_riigid">

    <TabBar>
        <ShellContent
            Title="Notes"
            ContentTemplate="{DataTemplate local:NotePage}"
            Icon="{OnPlatform 'location.png'}"
            Route="StartPage" />

        <ShellContent
            Title=""
            ContentTemplate="{DataTemplate local:EuroopaRiigidPage}"
            Icon="{OnPlatform 'teku.png'}"
            Route="StartPage" />

        <ShellContent
            Title="Lipud"
            ContentTemplate="{DataTemplate local:NotePage}"
            Icon="{OnPlatform 'red_flag.png'}"
            Route="StartPage" />

        <ShellContent
            Title="Lipud"
            ContentTemplate="{DataTemplate local:EuroopaRiigidPage}"
            Icon="{OnPlatform 'red_flag.png'}"
            Route="StartPage" />

        <ShellContent
            Title="Lipud"
            ContentTemplate="{DataTemplate local:NotePage}"
            Icon="{OnPlatform 'red_flag.png'}"
            Route="StartPage" />


    </TabBar>


</Shell>
